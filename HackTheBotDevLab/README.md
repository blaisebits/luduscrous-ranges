# STILL BUILDING, NO TOUCHY! NO WORKY RIGHT NOW....Y

HackTheBot Dev Lab!

# Setup
1. Clone the repo
2. Add the following ansible roles using `ludus ansible role add $ROLE`
  1. `geerlingguy.docker`
  2. `webofmars.xfce4-desktop`
  3. `Oefenweb.pycharm`
  4. `bartdorlandt.poetry`
3. customize config
4. import the range config
5. build lab